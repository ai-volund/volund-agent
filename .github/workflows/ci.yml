name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read
  packages: write

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          path: volund-agent
      - uses: actions/checkout@v4
        with:
          repository: ai-volund/volund-proto
          path: volund-proto
      - uses: actions/setup-go@v5
        with:
          go-version: '1.26'
      - name: Create go.work
        run: |
          go work init
          go work use ./volund-agent ./volund-proto
      - uses: golangci/golangci-lint-action@v6
        with:
          version: latest
          working-directory: volund-agent

  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          path: volund-agent
      - uses: actions/checkout@v4
        with:
          repository: ai-volund/volund-proto
          path: volund-proto
      - uses: actions/setup-go@v5
        with:
          go-version: '1.26'
      - name: Create go.work
        run: |
          go work init
          go work use ./volund-agent ./volund-proto
      - run: go test -race -coverprofile=coverage.out ./...
        working-directory: volund-agent
      - uses: actions/upload-artifact@v4
        with:
          name: coverage
          path: volund-agent/coverage.out

  build:
    runs-on: ubuntu-latest
    needs: [lint, test]
    steps:
      - uses: actions/checkout@v4
        with:
          path: volund-agent
      - uses: actions/checkout@v4
        with:
          repository: ai-volund/volund-proto
          path: volund-proto
      - uses: actions/setup-go@v5
        with:
          go-version: '1.26'
      - name: Create go.work
        run: |
          go work init
          go work use ./volund-agent ./volund-proto
      - run: make build
        working-directory: volund-agent

  docker:
    runs-on: ubuntu-latest
    needs: [build]
    if: github.ref == 'refs/heads/main'
    steps:
      - uses: actions/checkout@v4
      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - uses: docker/build-push-action@v6
        with:
          push: true
          tags: |
            ghcr.io/ai-volund/volund-agent:latest
            ghcr.io/ai-volund/volund-agent:${{ github.sha }}
